# 首页布局开发中

## 当前目标

- 固化 `payapp2` 首页当前已确认版本的布局与图标基线，作为后续改动和回归对照标准。

## 当前结论

- 用户已明确确认：首页当前整体布局效果“完美”，应视为当前视觉基线。
- 首页服务宫格图标应满足三个硬要求：完整显示、视觉居中、不同图标位置一致。
- 首页服务图标当前统一走 `public/assets/service-icons-normalized/`，不再使用之前会导致留白和裁切不一致的旧资源组。
- `政府促消费` 与 `赚钱中心` 已做单独视觉修正，允许保留局部偏移，不强行套统一自动居中规则。
- `/mnt/d/Ax/code/payapp2` 在 WSL 下的 `vite` 热更新存在漏读修改的情况，验证页面效果前应优先确认 dev 服务是否已重启并返回最新源码。
- 真机 APK 不应再渲染任何前端“假状态栏”时间/信号文本；状态栏显示以手机系统原生状态栏为准。
- 真机 APK 的各页面顶部都需要统一预留一小段系统状态栏安全区，不能只让个别页面单独顶开。
- 首页顶部红区主图允许按用户指定图片替换，但必须保证主图完整显示在四个顶部白色图标下方，且不能与下方九宫格重叠。

## 当前状态

### 已完成

- 建立项目文档骨架：任务板、规则、文件索引、交接、决策记录。
- 记录首页布局当前基线结论。
- 记录首页服务图标资源归一化方案与验证入口。

### 进行中

- 无代码开发任务进行中。

### 下一步

- 后续若继续调整首页，只允许基于当前基线做小范围对比式修改。
- 若用户再次确认某一版视觉效果，应同步覆盖更新本任务板和对应决策文档。

## 最近关键改动

- 首页服务图标资源切换到 `service-icons-normalized`。
- 为 `政府促消费`、`赚钱中心` 做了单独视觉修正。
- 2026-05-29 删除首页与“我的小程序”页中的前端假状态栏节点，避免与真机系统状态栏重叠显示。
- 2026-05-29 为搜索、优惠、卡包、财富、我的页头统一补上 Android 状态栏安全区留白，和当前正常页面保持同一处理方式。
- 2026-05-29 首页顶部主图改为用户提供的自定义红区图，同时缩小“鞍山”、放大搜索框右侧“搜索”，并抬高顶部红区避免主图与九宫格重叠。
- 按用户指定 APK `C:\Users\12196\Downloads\payapp2-rollback-home-actions-lower.apk` 全量还原运行态资源：已用该 APK 内 `assets/www` 替换 `app/src/main/assets/www` 和 `dist`；已用本地 Gradle 8.5 打包成功，并输出 `C:\Users\12196\Downloads\payapp2-restored-to-rollback-home-actions-lower.apk`；新 APK 内 `index.html`、主 JS、主 CSS 与指定 APK 对比一致。
- 2026-05-29 再次以 `C:\Users\12196\Downloads\payapp2-rollback-home-actions-lower.apk` 作为唯一基线恢复：`dist` 与 `app/src/main/assets/www` 已和该 APK 的 `assets/www` 做 `diff -qr` 校验无差异；`MainActivity.kt` 已恢复为该 APK 对应的透明状态栏 + `--android-statusbar-height` 注入方案；重新输出 `C:\Users\12196\Downloads\payapp2-restored-to-rollback-home-actions-lower.apk`。
- 明确了 `/mnt/d` 下 `vite` 热更新不稳定时需要重启 dev 服务再验证。

## 未决问题

- 当前无未决业务问题。

## 风险点

- 如果后续直接替换首页图标资源而不做统一画布归一化，极易再次出现主体偏移、留白不一致、底部被截断的问题。
- 在 `/mnt/d` 路径下仅依赖 Vite HMR 观察效果有误判风险，必须结合源码返回内容或重启服务验证。
