# 首页布局开发中

## 当前目标

- 固化 `payapp2` 首页当前已确认版本的布局与图标基线，作为后续改动和回归对照标准。

## 当前结论

- 用户已明确确认：首页当前整体布局效果“完美”，应视为当前视觉基线。
- 首页服务宫格图标应满足三个硬要求：完整显示、视觉居中、不同图标位置一致。
- 首页服务图标当前统一走 `public/assets/service-icons-normalized/`，不再使用之前会导致留白和裁切不一致的旧资源组。
- `政府促消费` 与 `赚钱中心` 已做单独视觉修正，允许保留局部偏移，不强行套统一自动居中规则。
- `/mnt/d/Ax/code/payapp2` 在 WSL 下的 `vite` 热更新存在漏读修改的情况，验证页面效果前应优先确认 dev 服务是否已重启并返回最新源码。

## 当前状态

### 已完成

- 建立项目文档骨架：任务板、规则、文件索引、交接、决策记录。
- 记录首页布局当前基线结论。
- 记录首页服务图标资源归一化方案与验证入口。

### 进行中

- 无代码开发任务进行中。

### 下一步

- 后续若继续调整首页，只允许基于当前基线做小范围对比式修改。
- 若用户再次确认某一版视觉效果，应同步覆盖更新本任务板和对应决策文档。

## 最近关键改动

- 首页服务图标资源切换到 `service-icons-normalized`。
- 为 `政府促消费`、`赚钱中心` 做了单独视觉修正。
- 明确了 `/mnt/d` 下 `vite` 热更新不稳定时需要重启 dev 服务再验证。

## 未决问题

- 当前无未决业务问题。

## 风险点

- 如果后续直接替换首页图标资源而不做统一画布归一化，极易再次出现主体偏移、留白不一致、底部被截断的问题。
- 在 `/mnt/d` 路径下仅依赖 Vite HMR 观察效果有误判风险，必须结合源码返回内容或重启服务验证。
